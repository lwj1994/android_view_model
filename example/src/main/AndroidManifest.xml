<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:name=".ExampleApplication"
        android:allowBackup="false"
        android:label="AndroidViewModel Example"
        android:supportsRtl="true"
        android:theme="@style/ExampleTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RemoteProcessActivity"
            android:exported="false"
            android:process=":remote" />
        <provider
            android:name=".ProcessCounterStateProvider"
            android:authorities="${applicationId}.process-counter-state"
            android:exported="false"
            android:process=":state_store" />
    </application>
</manifest>
